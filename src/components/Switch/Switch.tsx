import React from 'react'
import styles from './Switch.styles'
import { SwitchProps } from 'types'

const Switch = ({ state = true, leftText, rightText, onChange }: SwitchProps) => {

  return (
    
  )
}

export default Switch
