import { createSelector } from 'reselect'
import get from 'lodash/get'

// export const dashboardSelector = state => get(state, 'dashboard', {})

// export const statsFiltersSelector = createSelector(
//   dashboardSelector,
//   dashboard => dashboard.statsFilters || {},
// )
